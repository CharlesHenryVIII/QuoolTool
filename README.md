# ScadaBackup

### Building
* Run GenerateProjectFiles.bat
* open the VS solution
* Build/run from there

* To setup the build environment you must have vcpkg installed
    * clone the repo
    * run bootstrap-vcpkg.bat
    * vcpkg integrate install
* vcpkg install libarchive:x64-windows-static
